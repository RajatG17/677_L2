version: "3.9"
services:
  front-end:
    build: 
      context: .
      dockerfile: ./frontend.Dockerfile
    ports:
      - "4000:4000"
    depends_on:
      - catalog
      - order
  catalog:
    build:
      context: .
      dockerfile: ./catalog.Dockerfile 
    volumes:
      - ./src/Part1/data:/root-dir/data
  order:
    build:
      context: .
      dockerfile: ./order.Dockerfile
    volumes:
      - ./src/Part1/data:/root-dir/data
    depends_on:
      - catalog
